{
  "name": "v2_subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ v2_subgraph",
    "create-local": "graph create --node http://localhost:8020/ v2_subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ v2_subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 v2_subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.85.0",
    "@graphprotocol/graph-ts": "0.32.0",
    "ethers": "^6.13.3"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
